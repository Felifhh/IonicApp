import { ComponentFixture, TestBed } from '@angular/core/testing';
import { IngresecorreoPage } from './ingresecorreo.page';

describe('IngresecorreoPage', () => {
  let component: IngresecorreoPage;
  let fixture: ComponentFixture<IngresecorreoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(IngresecorreoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
